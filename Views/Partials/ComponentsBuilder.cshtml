@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<IEnumerable<Component>>
@{
    foreach (var component in Model)
    {
        @Html.Partial($"Partials/Components/{component.Alias}", component);
    }
}